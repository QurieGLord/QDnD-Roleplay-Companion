# Git Worktree Setup - QD&D Project

## 📁 Структура директорий

Проект QD&D использует **git worktree** для одновременной работы с несколькими ветками:

```
/home/qurie/Dev/Flutter/
├── qd_and_d-main/       # Основной репозиторий (ветка: main)
├── qd_and_d-claude/     # Worktree для Claude Code (ветка: claude)
└── qd_and_d-gemini/     # Worktree для Gemini AI (ветка: gemini)
```

## 🌿 Ветки

### `main` - Stable Releases
- **Директория**: `/home/qurie/Dev/Flutter/qd_and_d-main`
- **Назначение**: Стабильная ветка с проверенным кодом
- **Правила**: Мержи только после ревью и тестирования

### `claude` - Claude Code Workspace ⚠️
- **Директория**: `/home/qurie/Dev/Flutter/qd_and_d-claude`
- **Назначение**: Рабочая ветка для Claude Code
- **Правила**: Все изменения от Claude делаются здесь

### `gemini` - Gemini AI Workspace
- **Директория**: `/home/qurie/Dev/Flutter/qd_and_d-gemini`
- **Назначение**: Рабочая ветка для Gemini AI
- **Правила**: Все изменения от Gemini делаются здесь

## 🔧 Команды Git Worktree

### Просмотр всех worktree
```bash
cd /home/qurie/Dev/Flutter/qd_and_d-main
git worktree list
```

### Добавление нового worktree
```bash
git worktree add <path> <branch>
# Пример:
git worktree add /home/qurie/Dev/Flutter/qd_and_d-new new-branch
```

### Удаление worktree
```bash
git worktree remove <path>
# Или:
cd <path>
cd ..
rm -rf <path>
git worktree prune  # Очистить записи
```

### Перемещение worktree
```bash
git worktree move <old-path> <new-path>
```

## 💼 Workflow для Claude Code

### 1. Переключение в claude worktree
```bash
cd /home/qurie/Dev/Flutter/qd_and_d-claude
```

### 2. Проверка текущей ветки
```bash
git branch
# Должно показать: * claude
```

### 3. Работа с кодом
```bash
# Обычная работа - все команды git работают как в обычном репо
git status
git add .
git commit -m "message"
git push origin claude
```

### 4. Синхронизация с main
```bash
# Если нужно взять изменения из main
git fetch origin
git merge origin/main
# Или rebase:
git rebase origin/main
```

## ⚠️ ВАЖНО: Правила работы

### ✅ Можно делать:
- Работать в своей ветке (`claude` или `gemini`)
- Коммитить изменения в свою ветку
- Push в remote в свою ветку
- Создавать feature ветки от своей основной ветки

### ❌ Нельзя делать:
- Переключаться на ветки других worktree
- Коммитить напрямую в `main` без ревью
- Использовать `git push --force` в shared ветках
- Удалять worktree другого AI

## 🔄 Синхронизация между ветками

### Мерж из claude в main
```bash
# 1. Перейти в main worktree
cd /home/qurie/Dev/Flutter/qd_and_d-main

# 2. Убедиться что на ветке main
git checkout main

# 3. Мержить из claude
git merge claude

# 4. Push в remote
git push origin main
```

### Мерж из main в claude
```bash
# 1. Перейти в claude worktree
cd /home/qurie/Dev/Flutter/qd_and_d-claude

# 2. Убедиться что на ветке claude
git branch

# 3. Мержить из main
git merge main

# 4. Push в remote
git push origin claude
```

## 📊 Статус worktree

### Текущий статус
```bash
git worktree list
```

Вывод:
```
/home/qurie/Dev/Flutter/qd_and_d-main    da3ac4d [main]
/home/qurie/Dev/Flutter/qd_and_d-claude  30f4685 [claude]
/home/qurie/Dev/Flutter/qd_and_d-gemini  1512422 [gemini]
```

## 🎯 Преимущества worktree

1. **Одновременная работа** с разными ветками без переключения
2. **Изоляция изменений** - каждый AI работает в своей директории
3. **Быстрое переключение** между ветками (просто `cd`)
4. **Нет конфликтов** - разные worktree не влияют друг на друга
5. **Удобное тестирование** - можно запустить разные версии одновременно

## 📝 Примечания

- Все worktree используют **один общий .git** репозиторий из `qd_and_d-main`
- Изменения в одном worktree видны в `git log` других worktree
- Файл `.git` в worktree - это ссылка на основной репозиторий
- При удалении worktree ветка остаётся в репозитории

---

**Создано**: 2025-11-18
**Автор**: Claude Code Setup
**Проект**: QD&D - Quick D&D Companion
